#!/bin/bash
curl "https://jenkins.mesosphere.com/service/jenkins/view/Marathon/job/public-marathon-unstable/api/json?pretty=true&allBuilds=true" | jq '.builds | map(.number | tostring) | .[1:] | join(" ")' -r
